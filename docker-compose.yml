services:
  app:
    container_name: plex-discord-theater
    build:
      context: .
      args:
        VITE_DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID}
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - NODE_ENV=production
      - PLEX_URL=${PLEX_URL}
      - PLEX_TOKEN=${PLEX_TOKEN}
      - PLEX_ACCOUNT_TOKEN=${PLEX_ACCOUNT_TOKEN}
      - DISCORD_CLIENT_ID=${DISCORD_CLIENT_ID}
      - DISCORD_CLIENT_SECRET=${DISCORD_CLIENT_SECRET}
      - REDIRECT_URI=${REDIRECT_URI}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
      - ALLOWED_GUILD_IDS=${ALLOWED_GUILD_IDS}
      - VPS_RELAY_URL=${VPS_RELAY_URL}
      - VPS_RELAY_KEY=${VPS_RELAY_KEY}
      - SEERR_URL=${SEERR_URL}
      - TMDB_API_KEY=${TMDB_API_KEY}
      # Ratings row. Unset, /api/ratings answers configured:false and the row
      # simply never appears — which looked like a bug when the key was set in
      # .env but stopped here.
      - MDBLIST_API_KEY=${MDBLIST_API_KEY}
      - ADMIN_SECRET=${ADMIN_SECRET}
      # The bandwidth levers. Documented in .env.example as the biggest one
      # available, and unreachable from here until now.
      - VIDEO_BITRATE_KBPS=${VIDEO_BITRATE_KBPS}
      - VIDEO_PEAK_BITRATE_KBPS=${VIDEO_PEAK_BITRATE_KBPS}
      # Local media mount (optional): if you mount the library below at a different
      # path than Plex uses, map Plex's prefix → the mount. Unset if paths match.
      - MEDIA_PATH_MAP=${MEDIA_PATH_MAP}
      - WARM_CACHE=${WARM_CACHE}
      - WARM_CACHE_MAX_ITEMS=${WARM_CACHE_MAX_ITEMS}
      - WARM_CACHE_DELAY_MS=${WARM_CACHE_DELAY_MS}
      - WARM_CACHE_INTERVAL_MIN=${WARM_CACHE_INTERVAL_MIN}
      - LOG_TO_FILE=${LOG_TO_FILE}
      - LOG_DIR=${LOG_DIR}
      - LOG_MAX_FILE_MB=${LOG_MAX_FILE_MB}
      - LOG_RETENTION_DAYS=${LOG_RETENTION_DAYS}
      - THUMB_CACHE_TTL_MS=${THUMB_CACHE_TTL_MS}
      - THUMB_CACHE_MAX_MB=${THUMB_CACHE_MAX_MB}
      - PORT=${PORT:-3000}
      - THUMB_CACHE_DIR=/data
      # Set DEBUG=1 to turn on verbose HLS segment/session logging.
      - DEBUG=${DEBUG:-}
    volumes:
      - app-data:/data
      # ── Local media (optional, read-only) ────────────────────────────────
      # Mount your library so ffmpeg reads files off disk instead of over Plex's
      # HTTP URL — much faster, especially subtitle extraction. Auto-detected: if
      # the file isn't found here it falls back to HTTP. Mount at the SAME path
      # Plex reports (Part.file) and no MEDIA_PATH_MAP is needed; otherwise set
      # MEDIA_PATH_MAP above to rewrite Plex's prefix → this mount point.
      # - /path/to/media:/media:ro
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  app-data:
